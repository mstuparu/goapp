upstream goapp {
    {% for host in groups['tag_Function_goapp']  %}
    server {{ hostvars[host]['ec2_dns_name'] }};
    {% endfor %}
}


server {
        listen 80;

        location / {
            proxy_pass http://goapp;
        }
}
